---
title: use-geometry
description: Overview of the published use-geometry facade and the full current RustUse geometry set.
---

import CrateGrid from '../../../components/CrateGrid.astro';
import CrateSourcePanel from '../../../components/CrateSourcePanel.astro';
import DocsMascotPanel from '../../../components/DocsMascotPanel.astro';
import MascotImportant from '../../../components/MascotImportant.astro';
import MascotTip from '../../../components/MascotTip.astro';
import StatusBadge from '../../../components/StatusBadge.astro';
import { getPublicCratesBySet } from '../../../data/crates';

<DocsMascotPanel
	alt="A Rust mascot introducing the use-geometry set"
	eyebrow="Published set + facade"
	title="Geometry scales better when the facade stays thin."
	description={`use-geometry is the published facade for a broad geometry workspace that currently exposes ${getPublicCratesBySet('use-geometry').length} public crate tracks across foundations, primitives, transformations, relations, curves, regions, polytopes, configurations, complexes, and decomposition or folding crates.`}
	quickLinks={[
		{ href: '/sets/use-geometry/', label: 'use-geometry set' },
		{ href: '/use-geometry/use-point/', label: 'use-point' },
		{ href: '/use-geometry/use-line/', label: 'use-line' },
		{ href: '/api/workspaces/use-geometry/', label: 'workspace Rustdocs' },
	]}
	theme="clay"
	reverse
>
	<MascotTip slot="art" spriteOnly />
</DocsMascotPanel>

This page combines the `use-geometry` facade overview with the current public inventory for the RustUse geometry set.

<p>
	<StatusBadge status="published" /> Published facade crate with live registry
	links. <StatusBadge status="scaffolded" /> The geometry workspace's local
	`use-complex` crate currently stays on a RustUse-hosted workspace route.
</p>

<MascotImportant
	title="Published facade, broad child-crate taxonomy"
	description="The facade crate is published and reexports the current geometry child crates. Most child crates also use stable published package identities; the local geometry use-complex crate currently keeps a workspace-scoped RustUse docs route while its external package name remains unavailable."
/>

## Purpose

`use-geometry` is the feature-gated facade crate for the RustUse geometry workspace. The facade reexports focused child crates, exposes matching namespace modules, and keeps the shared geometry surface available through one dependency when that integration shape is clearer than depending on child crates individually.

## Overview

The current `use-geometry` set surfaces {getPublicCratesBySet('use-geometry').length} public crate tracks: the `use-geometry` facade plus its focused child crates. The set is intentionally broad inside pure geometry, but the facade remains thin: implementation lives in focused crates with explicit boundaries.

## Taxonomy

| Group                                              | Crates                                                                                                                                                |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Foundations                                        | `use-coordinate`, `use-dimension`, `use-angle`, `use-point`                                                                                           |
| Affine and Euclidean primitives                    | `use-bounds`, `use-line`, `use-ray`, `use-segment`, `use-plane`, `use-hyperplane`, `use-circle`, `use-sphere`, `use-hypersphere`, `use-torus` |
| Transformations                                    | `use-transform`, `use-affine`, `use-projection`, `use-reflection`, `use-inversion`                                                                    |
| Metric and relational geometry                     | `use-distance`, `use-orientation`, `use-intersection`, `use-containment`, `use-congruence`, `use-similarity`, `use-dihedral`                          |
| Curves and conics                                  | `use-conic`, `use-curve`, `use-polyline`, `use-bezier`, `use-spline`                                                                                  |
| Surfaces and manifolds                             | `use-surface`, `use-manifold`                                                                                                                         |
| Planar regions                                     | `use-triangle`, `use-rectangle`, `use-polygon`                                                                                                        |
| Polytopes and solids                               | `use-simplex`, `use-orthotope`, `use-polytope`, `use-polyhedron`, `use-regular-polytope`, `use-archimedean`, `use-catalan-solid`, `use-johnson-solid` |
| Polytope notation and classification               | `use-schlafli`, `use-wythoff`, `use-coxeter`, `use-uniform-polytope`                                                                                  |
| Incidence, projective geometry, and configurations | `use-incidence`, `use-projective`, `use-configuration`, `use-duality`                                                                                 |
| Representations and complexes                      | `use-mesh`, `use-face`, `use-cell`, `use-complex`                                                                                                     |
| Constructions and decompositions                   | `use-hull`, `use-triangulation`, `use-tessellation`, `use-voronoi`, `use-delaunay`                                                                    |
| Folding and unfolding                              | `use-folding`, `use-crease`, `use-origami`, `use-linkage`, `use-unfolding`, `use-rigid-folding`, `use-polyhedral-net`                                 |

## Public crate tracks

<CrateGrid crates={getPublicCratesBySet('use-geometry')} />

## API reference

- [use-geometry workspace Rustdocs](/api/workspaces/use-geometry/) - generated API bundle for the full geometry workspace.
- [use-geometry API reference](/api/use-geometry/) - stable RustUse API route for the facade crate.
- Published geometry child crates use stable `/api/<crate>/` routes from the catalog and sidebar.
- The local geometry `use-complex` crate currently uses the workspace-scoped route `/api/workspaces/use-geometry/use_complex/` so the docs do not point at the wrong published package identity.

<CrateSourcePanel
  crateName="use-geometry"
	description="Copy the published geometry facade crate as a full source bundle or browse the files that define the current reexport surface."
/>

## Placement notes

- The facade crate only reexports child crates and shared namespace modules.
- `use-line` is the published package name for line primitives; its Rust library name stays `use_line`.
- `use-vector` remains in the sibling `use-math` workspace and is used by geometry child crates that need vector primitives.
- `use-geode` remains in `use-math` and is not part of the geometry set.
- The local geometry `use-complex` crate stays in the workspace taxonomy, but its external package identity is currently blocked, so RustUse keeps it on a workspace-scoped internal docs route.

## Status

The `use-geometry` facade is published, and most current child crates in the
set are also published. RustUse-hosted generated Rustdocs remain canonical, and
workspace-scoped routes cover the remaining identity edge cases without reusing
the wrong external package name.
let triangle = Triangle::try_new(
	Point2::try_new(0.0, 0.0).expect("valid vertex"),
	Point2::try_new(4.0, 0.0).expect("valid vertex"),
	Point2::try_new(0.0, 3.0).expect("valid vertex"),
)
.expect("valid triangle");

assert_eq!(triangle.area(), 6.0);
assert_eq!(triangle.perimeter(), 12.0);
```

### Working with trusted coordinates

```rust
use use_geometry::{Point2, Triangle};

let triangle = Triangle::new(
	Point2::new(0.0, 0.0),
	Point2::new(4.0, 0.0),
	Point2::new(0.0, 3.0),
);

assert_eq!(triangle.sides(), [4.0, 5.0, 3.0]);
```

### Checking exact vs tolerance-based degeneracy

```rust
use use_geometry::{Point2, Triangle};

let triangle = Triangle::try_new(
	Point2::try_new(0.0, 0.0).expect("valid vertex"),
	Point2::try_new(4.0, 0.0).expect("valid vertex"),
	Point2::try_new(8.0, 1.0e-13).expect("valid vertex"),
)
.expect("valid triangle");

assert!(!triangle.is_degenerate());
assert_eq!(triangle.is_degenerate_with_tolerance(1.0e-12), Ok(true));
```

## Status

`use-geometry` is a scaffolded public crate and set anchor in the RustUse docs surface. The API remains pre-1.0, and the RustUse-hosted generated rustdocs stay canonical while external crates.io and docs.rs pages remain staged.
