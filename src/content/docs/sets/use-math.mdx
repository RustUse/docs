---
title: use-math
description: The published RustUse math set centered on the use-math facade and its focused math crates.
---

import CrateGrid from '../../../components/CrateGrid.astro';
import MascotNote from '../../../components/MascotNote.astro';
import StatusBadge from '../../../components/StatusBadge.astro';
import { getPublicCratesBySet } from '../../../data/crates';

`use-math` is the current RustUse math set. It currently surfaces {getPublicCratesBySet('use-math').length} public crate tracks: the published `use-math` facade plus its focused math crates.

<MascotNote
  title="Published math set"
  description="The public use-math set is now driven from the live crate inventory. RustUse-hosted generated rustdocs remain canonical, and published crates now expose live crates.io and docs.rs links."
/>

<p>
  <StatusBadge status="published" /> All current public crates in this set are
  published today.
</p>

## Purpose

This set keeps a thin facade over focused math crates so callers can choose between one dependency and a narrower direct crate for a specific math domain. Geometry now lives in the sibling `use-geometry` set.

## Public crate tracks

<CrateGrid crates={getPublicCratesBySet('use-math')} />

## Generated Rustdocs

- [use-math workspace Rustdocs](/api/workspaces/use-math/) - generated API bundle for the full math workspace.
- [use-math API reference](/api/use-math/) - stable RustUse API route for the facade crate.
- Every focused crate in this set also has a stable `/api/<crate>/` route. Use the crate grid above or the sidebar for direct entry points.

## Catalog and guidance

- [Crates](/crates/) - current public crate catalog with shared status badges and link targets.
- [API Reference](/api-reference/) - RustUse.org API strategy, Cargo metadata guidance, and future rustdoc JSON notes.

## Why this shape

Keeping the facade thin and the focused crates direct makes the set easier to adopt incrementally while preserving one-dependency ergonomics where they help.

## Status

The current public math set contains {getPublicCratesBySet('use-math').length}
published crates. RustUse-hosted generated rustdocs remain canonical while
crates.io and docs.rs stay live as registry and fallback references.
